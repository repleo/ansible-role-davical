---
- hosts: localhost
  remote_user: root
  roles:
    - { role: ansible-role-davical,
         rainloop_db_password: P4SSword,
         rainloop_db_host: localhost,
         rainloop_db_name: davical,
    }
